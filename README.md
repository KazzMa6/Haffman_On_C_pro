# Haffman_On_C_pro
# 📦 Huffman Unicode Архиватор на C (Windows, UTF-8)

Проект реализует **архивацию текста по алгоритму Хаффмана** с поддержкой **русского языка и Unicode-символов**. Написан на языке **C** с учетом особенностей Windows и кодировки UTF-8.

---

## 🧩 Что делает программа?

🔹 Считывает текст из файла `input.txt` (UTF-8)  
🔹 Строит дерево Хаффмана на основе частоты символов  
🔹 Генерирует уникальные коды символов (префиксные)  
🔹 Кодирует текст в битовую строку  
🔹 Декодирует обратно в оригинальный текст  
🔹 Выводит всё в `output.txt`:
  - коды символов;
  - зашифрованный текст;
  - результат обратного восстановления.

---

## 🖼 Пример

### `input.txt`:

Привет, мир!
Алгоритм Хаффмана работает.

### `output.txt`:

--- Коды символов ---
'П': 101
'р': 0010
'и': 111
...

--- Закодированный текст ---
1010010111000110111...

--- Декодированный текст ---
Привет, мир!
Алгоритм Хаффмана работает.

## ⚙️ Как собрать и запустить

### 1. Установи компилятор (если ещё не установлен)
- Рекомендуется: [MinGW-w64](https://www.mingw-w64.org/) или [TDM-GCC](https://jmeubank.github.io/tdm-gcc/)

### 2. Скомпилируй программу:

```bash
gcc -o huffman huffman_unicode_windows.c -Wall

Убедись, что файл действительно в UTF-8, без BOM. Лучше использовать редакторы VS Code или Notepad++.



Как работает алгоритм Хаффмана:

Частотный анализ: Подсчитывается количество вхождений каждого символа.
Мини-куча: Используется структура данных “min-heap” для сортировки узлов по частоте.
Построение дерева: Самые редкие символы объединяются первыми.
Генерация кодов: Символам присваиваются уникальные префиксные коды (0 и 1).
Кодирование: Исходный текст заменяется на соответствующие коды.
Декодирование: Битовая строка декодируется обратно, проходя по дереву.

Особенности реализации:

Поддержка русского и любого языка (через wchar_t)
Файлы читаются и пишутся в UTF-8 с помощью _wfopen(..., ccs=UTF-8)
Структура кода разбита на:
Построение дерева
Построние кодов
Кодирование
Декодирование
Вывод таблицы кодов для каждого символа
Встроенное восстановление оригинального текста

Совместимость

| Операционная система | Поддержка                      | Примечания                               |
|---------------------|-------------------------------|-----------------------------------------|
| Windows          | Полностью совместимо (MinGW) | Работает из коробки с поддержкой UTF-8  |
| Linux            | Требуется портирование       | Нужно адаптировать ввод/вывод для wchar_t |
| macOS            | Аналогично                  | Возможны проблемы с `wchar_t` и локалями |


Возможности для расширения:

 Сохранение дерева Хаффмана в файл (для декодирования без перекодирования)
 Сжатие в бинарный формат .bin (с выводом не символов 0/1, а байтов)
 Командная строка с параметрами:
--encode input.txt
--decode encoded.txt
 Поддержка Linux/macOS
 Интерфейс GUI
